#!/bin/bash

module purge
module load NVHPC/24.5-CUDA-12.4.0 CMake Ninja

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

#export PATH=${SCRIPT_DIR}/tools/llvm/bin:${PATH}

